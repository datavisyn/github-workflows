name: release-source

on:
  workflow_call:

permissions:
  contents: write

concurrency:
  group: '${{ github.workflow }}-${{ github.ref || github.head_ref }}'
  cancel-in-progress: true

env:
  GA_VERSION: "main"
  GITHUB_USER: "datavisyn-bot"
  WORKFLOW_BRANCH: "main"
  GITHUB_TOKEN: ${{ secrets.REPO_TOKEN }}

jobs:
  release-repository:
    runs-on: ubuntu-22.04
    steps:
      - name: check branch
        if: ${{ github.ref_name != 'develop' }}
        run: |
          echo 'only the develop branch can be released.'
          exit 1
      # checkout specific repository
      - name: checkout repository
        uses: actions/checkout@v3
        with:
          token: ${{ github.token }}
      # checkout this workflow repository to get actions
      - name: checkout github-workflows repository
        uses: actions/checkout@v3
        with:
          repository: datavisyn/github-workflows
          ref: ${{ env.WORKFLOW_BRANCH }}
          path: ./tmp/github-workflows
      - name: check actor
        uses: ./tmp/github-workflows/.github/actions/check-actor
        with:
          dv_devops: ${{ vars.DV_DEVOPS }}
          actor: ${{ github.actor }}
          qms_are_allowed: "false"
      - name: get release version
        id: get-release-version
        run: |
          release_version=$(jq -r '.version' package.json | cut -d '-' -f 1)
          echo $release_version
          echo "RELEASE_VERSION=$release_version" >> $GITHUB_ENV
      - name: create release branch
        run: |
          git config user.name "$GITHUB_ACTOR"
          git config user.email "<>"
          git checkout -b release/$RELEASE_VERSION
          git push origin release/$RELEASE_VERSION
      - name: Update package.json
        run: |
          jq '.version = env.RELEASE_VERSION' package.json > package.json.tmp
          mv package.json.tmp package.json
          git add package.json
          git commit -m "Update package.json version to $RELEASE_VERSION"
          git push origin release/$RELEASE_VERSION
      - name: change chart version
        if: ${{ github.repository }} == 'datavisyn/testapp' || ${{ github.repository }} == 'datavisyn/testapp2'
        run: |
          cd deploy/helm
          version=8.0.3
          ls_result=$(ls)
          echo $ls_result
          for dir in $ls_result; do sed -i "s/^version.*/version: $version/" $dir/chart.yaml; sed -i "s/^appVersion.*/appVersion: \"$version\"/" $dir/chart.yaml; done
          for dir in $ls_result; do cat $dir/chart.yaml; done
          git add .
          git commit -m "Update chart version to $version"
          git push origin release/$RELEASE_VERSION
          
             
      # - name: create pr
      #   id: create-pr
      #   run: |
      #     # create a pull request
      #     curl -L \
      #       -X POST \
      #       -H "Accept: application/vnd.github+json" \
      #       -H "Authorization: Bearer ${{ env.GITHUB_TOKEN}}" \
      #       -H "X-GitHub-Api-Version: 2022-11-28" \
      #       https://api.github.com/repos/${{ github.repository_owner }}/${{ github.event.repository.name }}/pulls \
      #       -d '{"title":"Release ${{ env.RELEASE_VERSION }}","body":"release ${{ env.RELEASE_VERSION }}","head":"release/${{ env.RELEASE_VERSION }}","base":"main","draft":false}'
          


      
 
        

            